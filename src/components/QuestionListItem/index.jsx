import { getQuestions, postReaction } from '../../api';
// import { getSubject } from '../../api'; //피드주인확인용..?

function QuestionListItem({ questionId, subjectName, subjectProfile }) {
  return <></>;
}

export default QuestionListItem;
